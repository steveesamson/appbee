
import { ViewConfig } from "../../../src";

const view: ViewConfig = {
        staticDir: 'public',
        viewDir:'views',
        uploadDir:'uploads',
        indexFile:'index'
};

export = view;
