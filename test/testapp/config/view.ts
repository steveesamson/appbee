
import { ViewConfig } from "../../../src";

const view: ViewConfig = {
        engine : 'stud',
        staticDir: 'public',
        viewDir:'views',
        uploadDir:'uploads',
        indexFile:'index'
};

export default view;
