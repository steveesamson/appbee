
import { ViewConfig } from "../../../src";

const view: ViewConfig = {
        staticDir: 'public',
        uploadDir:'uploads',
        indexFile:'index',
        templateDir:'template'
};

export default view;
