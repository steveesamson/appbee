
import type { ViewConfig } from "$lib/common/types.js";

const view: ViewConfig = {
        engine: 'stud',
        staticDir: 'public',
        viewDir: 'views',
        indexFile: 'index'
};

export default view;
