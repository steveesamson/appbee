import { Routes, ControllerRequest } from "../common/types";

/*
const makePath = (path: string, method: string): string => `${method} ${path}`;

class Route {
  routes: Routes = {};

  constructor() {
    this.routes = {};
  }
  route(path: string, method = "get", handler: ControllerRequest): void {
    this.routes[makePath(path, method)] = handler;
  }
  get(path: string, handler: ControllerRequest): Route {
    this.route(path, "get", handler);
    return this;
  }
  post(path: string, handler: ControllerRequest): Route {
    this.route(path, "post", handler);
    return this;
  }

  put(path: string, handler: ControllerRequest): Route {
    this.route(path, "put", handler);
    return this;
  }

  delete(path: string, handler: ControllerRequest): Route {
    this.route(path, "delete", handler);
    return this;
  }
  patch(path: string, handler: ControllerRequest): Route {
    this.route(path, "patch", handler);
    return this;
  }

  head(path: string, handler: ControllerRequest): Route {
    this.route(path, "head", handler);
    return this;
  }

  new(): Route {
    this.routes = {};
    return this;
  }
  map(): Routes {
    return this.routes;
  }
}
const router = (): Route => new Route();

export { router };

*/
